# language: zh-TW
功能: 地圖背景視覺強化
  作為玩家
  我希望三張地圖各有獨特的地貌與氛圍動畫
  以便獲得更有沉浸感、更易辨識的場景體驗

  # 註：呈現層功能，地貌/動畫以實機目視為主；bgHash 純函式以單元測試驗證。

  背景:
    假設 遊戲以固定種子開始並進入遊玩

  # ---------- Happy Path（實機目視） ----------
  場景: 三張地圖各具專屬地貌
    當 玩家分別在平原 / 熔岩 / 冰原遊玩
    那麼 平原呈現淡綠草叢與碎石
    並且 熔岩呈現岩裂折線與餘燼亮點
    並且 冰原呈現冰裂折線與雪堆

  場景: 氛圍動畫運作
    當 場景持續呈現
    那麼 熔岩餘燼脈動發光、冰原雪點緩緩飄落、平原草屑緩慢飄移
    並且 動畫流暢、不掉 FPS

  場景: 無限捲動穩定重現
    當 玩家朝某方向移動再折返
    那麼 同一世界座標的地面地貌維持一致（不閃爍、不重排）

  # ---------- Boundary Conditions ----------
  場景: 可視範圍裁切
    當 背景每幀重畫
    那麼 只繪製玩家可視範圍內的格子
    並且 氛圍粒子維持固定上限、不隨時間累積

  # ---------- Validation Failure / Error Handling ----------
  場景: bgHash 確定性
    假設 bgHash 為純函式
    當 以相同 (gx, gy) 呼叫多次
    那麼 回傳值完全相同且落在 [0,1) 區間
    並且 不呼叫 Math.random() 或模擬 RNG

  # ---------- 行為保持 ----------
  場景: 玩法與模擬不變
    假設 背景為純視覺
    那麼 碰撞、生怪、難度倍率、相機、entity 造型完全不變
    並且 既有單元測試全數維持通過

  # ---------- Determinism ----------
  場景: 背景不影響確定性
    假設 地面地貌走 bgHash 純函式、氛圍動畫走 renderer clock
    那麼 相同 seed 的模擬結果不變

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
