# language: zh-TW
功能: 空間網格接入 World（碰撞效能重構）
  作為開發者
  我希望把敵人鄰近查詢改走空間網格
  以便在高敵數時維持效能，且不改變任何遊戲行為

  背景:
    假設 遊戲以固定種子開始
    並且 場上的隨機皆使用該局 seeded rng

  # ---------- 行為保持（Happy Path） ----------
  場景: 子彈只命中與其重疊的敵人
    假設 一隻敵人與子彈路徑重疊、另有數隻遠方敵人
    當 子彈飛行並結算命中
    那麼 只有重疊的那隻敵人被扣血
    並且 遠方敵人的 hp 不變

  場景: 接觸傷害只對與玩家重疊的敵人生效
    假設 一隻敵人與玩家重疊、另有遠方敵人
    當 推進一個固定步長
    那麼 玩家因重疊敵人而扣血
    並且 遠方敵人不影響玩家（語意與重構前一致）

  場景: 大蒜只傷害半徑內的敵人
    假設 一隻敵人在大蒜半徑內、一隻在半徑外
    當 推進一個固定步長
    那麼 半徑內敵人被扣血
    並且 半徑外敵人 hp 不變

  場景: 既有核心循環行為不變
    假設 一發子彈命中並擊殺一隻敵人
    當 World 結算該格
    那麼 敵人失效、擊殺數 +1、原地掉落經驗寶石（與重構前完全一致）

  # ---------- Boundary Conditions ----------
  場景: 空場時查詢回空、不結算
    假設 場上沒有任何敵人
    當 推進一個固定步長
    那麼 不發生任何碰撞結算
    並且 不丟出例外

  場景: 中途死亡的敵人不被重複結算
    假設 一隻敵人在本格已被擊殺（active=false）但仍在網格內
    當 後續查詢取得該敵人為候選
    那麼 結算迴圈以 active 檢查略過它

  # ---------- Determinism ----------
  場景: 相同種子產生相同結果
    假設 兩局使用相同 seed 並推進相同步數
    當 比較兩局的擊殺數與玩家 hp
    那麼 兩局完全一致（網格不改變確定性）

  # ---------- Error Handling ----------
  場景: 負座標查詢正常運作
    假設 敵人與玩家位於負座標區域
    當 進行鄰近查詢
    那麼 仍正確找出重疊敵人（Math.floor 分格一致）

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
