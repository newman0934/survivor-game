# language: zh-TW
功能: 多種敵人
  作為玩家
  我希望面對行為與數值各異、並隨時間升級的敵人
  以便獲得有層次的生存壓力與挑戰

  背景:
    假設 遊戲以固定種子開始
    並且 場上的隨機皆使用該局 seeded rng

  # ---------- Happy Path ----------
  場景: 開局只生成已解鎖的敵種
    假設 遊戲時間為 0 秒
    當 生怪系統選擇敵種
    那麼 只會選到「basic」或「swarm」
    並且 不會選到尚未解鎖的「tank」或「charger」

  場景: 較強敵種隨時間解鎖
    假設 遊戲時間已超過 45 秒
    當 生怪系統多次選擇敵種
    那麼 候選包含「tank」
    並且 超過 90 秒後候選包含「charger」

  場景: 各敵種套用對應數值
    當 以種類「tank」建立敵人
    那麼 該敵人的 hp、速度、傷害、半徑、xp 等於 tank 的定義值
    並且 其 enemyKind 為「tank」

  場景: 群襲小怪成群生成
    假設 生怪系統抽到「swarm」
    當 World 生成該批敵人
    那麼 一次生成 4 隻 swarm
    並且 它們的生成位置彼此略有偏移

  場景: 基礎追擊類敵人直線追玩家
    假設 一隻「basic」敵人位於玩家右側
    當 推進一個固定步長
    那麼 該敵人的速度方向朝向玩家

  # ---------- Charger 特殊行為 ----------
  場景: 衝刺者在走路相慢速朝玩家移動
    假設 一隻「charger」處於走路相
    當 推進一個固定步長
    那麼 其速度方向朝向玩家
    並且 速度大小約為走路速

  場景: 衝刺者進入衝刺時鎖定方向並加速
    假設 一隻「charger」即將由走路相跨入衝刺相
    當 推進跨越該相位邊界的固定步長
    那麼 其速度被設為當下朝玩家方向乘以衝刺速
    並且 在衝刺相期間不再重新轉向（維持該速度向量）

  # ---------- Boundary Conditions ----------
  場景: 加權隨機在已解鎖種類間分布
    假設 遊戲時間足以解鎖全部敵種
    當 以同一 rng 連續選種多次
    那麼 各種類出現比例大致反映其權重
    並且 不會選到任何未解鎖種類

  # ---------- Error Handling ----------
  場景: 未指定種類時建立基礎敵人
    當 呼叫 createEnemy 而未提供種類
    那麼 建立的敵人 enemyKind 為「basic」

  # ---------- Determinism ----------
  場景: 相同種子產生相同生怪序列
    假設 兩局使用相同 seed 並推進相同步數
    當 比較兩局生成的敵種與位置序列
    那麼 兩局序列完全一致

  # ---------- Regression（既有行為不被破壞） ----------
  場景: 任一敵種被擊殺仍掉落經驗寶石並記擊殺
    假設 場上一隻敵人 hp 被打到 0
    當 World 結算該格
    那麼 該敵人失效、擊殺數 +1、原地掉落一顆經驗寶石

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
