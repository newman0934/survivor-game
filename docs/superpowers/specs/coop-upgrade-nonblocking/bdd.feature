# language: zh-TW
功能: 非阻塞升級流程（多人合作 1B）
  作為多人合作的玩家
  我希望升級時世界不為我暫停、逾時自動選
  以便不打斷隊友，且各機器結果一致

  背景:
    假設 遊戲以固定種子開始

  # ---------- 多人：非阻塞 ----------
  場景: 多人玩家升級取得待選卡且世界不暫停
    假設 兩名玩家
    當 玩家 0 升一級
    那麼 pendingOfferFor(0) 回 3 張卡
    並且 世界仍可繼續 step（不暫停）

  場景: 逾時自動選第一張
    假設 玩家 0 有待選卡且未選
    當 經過 12 秒（逾時）
    那麼 自動套用待選的第一張
    並且 該玩家 pendingOffer 清空
    並且 該玩家 pendingLevelUps 減一

  場景: 逾時前主動選定
    假設 玩家 1 有待選卡
    當 在 12 秒內 chooseUpgrade(1, 某待選 id)
    那麼 套用該卡
    並且 玩家 1 pendingOffer 清空、pendingLevelUps 減一

  場景: 一次升多級逐張提供
    假設 兩名玩家
    當 玩家 0 一次取得足夠升兩級的經驗
    那麼 先提供第一張；選定/逾時後下一格再提供下一張

  場景: 升級浮層期間角色照常可動
    假設 多人玩家 0 有待選卡
    當 設定玩家 0 的移動輸入並 step
    那麼 玩家 0 仍會移動（非阻塞）

  # ---------- Validation ----------
  場景: chooseUpgrade 傳入非待選 id 安靜略過
    假設 玩家 0 有待選卡
    當 chooseUpgrade(0, '不存在的id')
    那麼 狀態不變（pendingOffer 與 pendingLevelUps 不動）

  # ---------- 死亡 ----------
  場景: 死亡玩家不再倒數/自動選
    假設 多人玩家 0 有待選卡後死亡
    當 繼續 step
    那麼 玩家 0 的待選不再倒數、不自動選

  # ---------- 單人零退化 ----------
  場景: 單人 processUpgrades 為 no-op
    假設 單一玩家
    當 玩家升級
    那麼 pendingOfferFor(0) 為 null（不走非阻塞流程）
    並且 既有 consumeLevelUp/applyUpgrade 暫停握手行為不變

  # ---------- Determinism ----------
  場景: 選項與自動選跨機確定性
    假設 兩局相同 seed、相同角色陣列、相同輸入
    當 比較升級選項序列與自動選結果
    那麼 兩局完全一致

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端引擎邏輯（網路屬 SP4）
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
