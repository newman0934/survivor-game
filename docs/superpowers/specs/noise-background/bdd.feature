# language: zh-TW
功能: 噪聲紋理視差背景
  作為玩家
  我希望三場景背景是連續有機的組織質感（而非向量點綴）
  以便獲得更精緻、有縱深的沉浸畫面

  背景:
    假設 噪聲背景為 renderer 呈現層、不碰模擬/確定性
    而且 噪聲層加在 world 之下、特徵與粒子疊於其上
    而且 既有 173 單元測試不受影響

  # 噪聲純函式（TDD）
  場景: fbm 給定 seed 確定性可重現
    假設 相同的 (x, y, seed)
    當 多次呼叫 fbm
    那麼 每次回傳相同數值
    而且 數值落在已知範圍（0..1）

  場景: 不同 seed 產生不同噪聲
    假設 相同 (x, y) 但不同 seed
    當 呼叫 fbm
    那麼 一般情況回傳不同數值

  # Happy Path（呈現，瀏覽器截圖驗證）
  場景: 三場景呈現有機組織底 + 視差
    假設 玩家在血管/胃/肺泡其一
    當 渲染背景
    那麼 顯示連續噪聲組織底（依場景 tint：暖紅/琥珀/藍綠）
    而且 玩家移動時深/中兩層以不同速度視差捲動

  場景: 保留地貌特徵與粒子
    假設 噪聲層取代調性底
    當 渲染背景
    那麼 紅血球/肺泡/酸泡等特徵與氛圍粒子仍顯示於噪聲之上

  場景: 中央暖核維持縱深
    假設 已移除向量 warmCore
    當 渲染背景
    那麼 中央仍略亮略暖、聚焦縱深

  # Error Handling
  場景: 噪聲生成失敗退回
    假設 噪聲紋理或 TilingSprite 建立失敗
    當 初始化噪聲背景
    那麼 try/catch 退回（特徵/粒子照常），不丟例外、不影響可玩性

  # 清理 / resize
  場景: 銷毀釋放紋理且冪等
    假設 噪聲背景已建立
    當 呼叫 destroy
    那麼 釋放生成的紋理、無累積洩漏

  場景: resize 貼合
    假設 噪聲背景已建立
    當 畫面尺寸改變
    那麼 視差層尺寸更新貼合、紋理不需重生

  # 不變項
  場景: 不影響模擬與既有測試
    假設 加入噪聲背景
    當 推進模擬
    那麼 引擎/store/確定性不變
    而且 既有 173 單元測試全綠
