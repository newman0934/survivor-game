# language: zh-TW
功能: 地圖背景精修
  作為玩家
  我希望血管/胃/肺泡三場景背景更有結構、色彩、細節與生命感
  以便獲得「身處器官內」的沉浸感與更精緻的畫面

  背景:
    假設 背景全在 sprites.ts 的 drawMapBackground 繪製
    而且 地貌位置走 bgHash（世界格點）、動畫走 renderer clock
    而且 不使用 Math.random、不碰引擎模擬/store

  # Happy Path
  場景: 三場景各有專屬結構深度層
    假設 玩家在血管/胃/肺泡其一
    當 渲染背景
    那麼 血管顯示血漿流紋寬帶 + 血管壁弧度
    而且 胃顯示大尺度黏膜皺褶脊
    而且 肺泡顯示大肺泡囊輪廓 + 支氣管分支線

  場景: 暖核漸層提升色溫減少死黑
    假設 任一場景
    當 渲染背景
    那麼 畫面中央略亮略暖、向外漸暗
    而且 與後製暈影互補、不過曝

  場景: 繪製順序由底而上
    假設 渲染背景
    當 drawMapBackground 執行
    那麼 依序繪製 結構層 → 暖核 → 色斑 → 地面特徵 → 氛圍粒子

  # 細節密度
  場景: 每場景新增特徵變體
    假設 三場景
    當 渲染地面特徵
    那麼 血管含纖維蛋白絲、胃含胃小凹、肺泡含微血管網（既有特徵保留）

  # 動態生命感
  場景: 背景大尺度元素隨 clock 緩動
    假設 場景背景已繪製
    當 clock 推進
    那麼 血漿流紋漂移、胃皺褶脊蠕動、肺泡囊呼吸縮放
    而且 幅度克制、不暈眩

  # Boundary Conditions
  場景: 無限捲動穩定重現
    假設 玩家持續移動
    當 鏡頭捲動
    那麼 地貌依世界格點穩定重現、不閃爍

  # 不變項
  場景: 確定性與既有測試不受影響
    假設 加入背景精修
    當 推進模擬
    那麼 引擎/store/確定性不變、模擬無 Math.random
    而且 既有 173 單元測試全綠
