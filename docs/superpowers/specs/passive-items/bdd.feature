# language: zh-TW
功能: 被動道具 / 更多升級分支
  作為玩家
  我希望透過升級卡收集並強化有等級、有上限的被動道具
  以便組合出更有策略深度的 build

  背景:
    假設 遊戲以固定種子開始
    並且 玩家起始不持有任何被動道具
    並且 場上的隨機皆使用該局 seeded rng

  # ---------- Happy Path ----------
  場景: 透過升級卡解鎖被動道具
    假設 玩家未持有「菠菜」
    當 玩家升級並選擇「新道具：菠菜」
    那麼 玩家持有「菠菜」Lv1
    並且 全域傷害乘區依菠菜第 1 級效果提升

  場景: 升級既有被動道具
    假設 玩家持有「菠菜」Lv1
    當 玩家升級並選擇「菠菜 Lv1→Lv2」
    那麼 菠菜等級變為 2
    並且 全域傷害乘區再依第 2 級效果提升

  場景: 護甲降低接觸傷害
    假設 玩家持有「護甲」使 armor 為 4
    並且 一隻接觸傷害為 5 的敵人與玩家重疊
    當 推進一個固定步長
    那麼 玩家受到的傷害以 max(0, 5 − 4) 計算

  場景: 回復每格回血但不超過最大血
    假設 玩家持有「番茄」使 regen 大於 0
    並且 玩家 hp 低於 maxHp
    當 推進若干固定步長
    那麼 玩家 hp 上升
    並且 hp 不超過 maxHp

  場景: 經驗加成放大撿寶經驗
    假設 玩家持有「皇冠」使 xpGain 大於 1
    當 玩家撿取一顆經驗寶石
    那麼 獲得的經驗為寶石 xp × xpGain

  場景: 最大血道具同步提升 maxHp 與 hp
    假設 玩家持有「空心之心」
    當 升級空心之心
    那麼 player.maxHp 增加
    並且 player.hp 同步增加相同量

  # ---------- Boundary Conditions ----------
  場景: 被動持有達上限後不再出現解鎖被動候選
    假設 玩家已持有 6 種被動道具
    當 觸發一次升級並產生候選
    那麼 候選中不包含任何「解鎖被動」選項

  場景: 被動滿級後不再出現該被動的升級候選
    假設 玩家的「菠菜」已達最高級
    當 觸發一次升級並產生候選
    那麼 候選中不包含「菠菜」的升級選項

  # ---------- Validation / 移除舊卡 ----------
  場景: 候選不再包含舊的無限乘區卡
    假設 任意升級狀態
    當 產生升級候選
    那麼 候選中不包含舊的 damage / firerate / projspeed / movespeed / pickup 乘區卡 id

  場景: 候選不足時以 heal 保底補滿
    假設 全部武器與被動皆已滿級或達上限
    當 觸發一次升級並產生候選
    那麼 候選恰為三張且以「補血」保底卡補滿

  # ---------- Error Handling ----------
  場景: 套用未知升級 id 時安靜略過
    當 以一個不存在的升級 id 呼叫套用
    那麼 不修改任何被動、武器或數值
    並且 不丟出例外

  # ---------- Determinism ----------
  場景: 相同種子產生相同升級候選序列
    假設 兩局使用相同 seed 與相同的升級選擇序列
    當 比較兩局每次升級產生的候選
    那麼 兩局的候選序列完全一致

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
