# language: zh-TW
功能: 整體調性/後製（全域視覺後製）
  作為玩家
  我希望遊戲畫面有泛光/色調/暈影的整體後製
  以便獲得更精緻、有電影感的視覺，且不犧牲效能與可玩性

  背景:
    假設 後製濾鏡套在 PixiJS app.stage
    而且 Vue 製的 HUD/選單/彈窗為獨立 DOM 疊層、不受濾鏡影響
    而且 後製不進入固定步長模擬迴圈、不影響確定性

  # Happy Path
  場景: 桌機套用完整後製
    假設 非觸控/coarse pointer 環境
    當 渲染器建立
    那麼 app.stage 套用泛光（AdvancedBloom）+ 色彩分級（ColorMatrix）
    而且 畫面顯示四角柔和壓暗的暈影

  場景: 亮部發散泛光
    假設 泛光已啟用
    當 場上有發光投射物/特效/抗原寶石
    那麼 這些亮部發散光暈、整體戰鬥畫面更亮更精緻

  # Boundary / 行動裝置降級
  場景: 行動裝置自動關閉泛光
    假設 coarse pointer（觸控）環境
    當 渲染器建立
    那麼 不建立泛光濾鏡
    而且 仍套用色彩分級與暈影

  場景: 無 matchMedia 的環境
    假設 環境無 matchMedia
    當 判定是否為 coarse pointer
    那麼 視為非 coarse（走桌機路徑）
    而且 不丟出例外

  # Error Handling
  場景: 濾鏡建立失敗時退回正常渲染
    假設 pixi-filters 載入或建立濾鏡失敗
    當 渲染器初始化後製
    那麼 以 try/catch 退回無濾鏡的正常渲染
    而且 遊戲照常可玩、不丟例外

  # 清理 / resize
  場景: 銷毀時清除後製且冪等
    假設 渲染器已套用後製
    當 呼叫 destroy 一次或多次
    那麼 濾鏡與暈影覆蓋物被清除
    而且 重複呼叫 destroy 安全、無洩漏

  場景: resize 時暈影重繪貼合
    假設 後製已套用
    當 畫面尺寸改變
    那麼 暈影覆蓋物重繪以貼合新尺寸
    而且 濾鏡不需重建

  # 不變項
  場景: 後製不影響模擬與既有測試
    假設 加入後製功能
    當 推進模擬
    那麼 引擎/store/確定性不變
    而且 既有 173 單元測試全綠
