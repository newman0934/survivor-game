# language: zh-TW
功能: 打擊反饋特效（A 批）
  作為玩家
  我希望擊殺、收集、升級、受傷都有明確的視覺反饋
  以便獲得更有打擊感與沉浸感的體驗

  # 註：呈現層功能，特效以實機目視為主；引擎不動、既有 122 測試維持全綠。

  背景:
    假設 遊戲以固定種子開始並進入遊玩

  # ---------- Happy Path（實機目視） ----------
  場景: 擊殺迸發粒子與環波
    當 敵人被擊殺
    那麼 在死亡位置以敵色迸發碎屑粒子並擴張一圈環形衝擊波
    並且 粒子在約 0.4 秒內淡出縮小回收

  場景: 收集寶石閃光
    當 玩家吸取經驗寶石
    那麼 在寶石位置出現亮綠星芒與擴張光圈

  場景: 升級光環
    當 玩家升級
    那麼 玩家身上擴張一圈金色光環與上升光點

  場景: 命中飄出傷害數字
    當 武器命中敵人
    那麼 在敵人位置上方飄出傷害值並向上淡出

  場景: 受傷紅暈與鏡頭震動
    當 玩家被敵人接觸扣血
    那麼 螢幕邊緣浮現紅色暈影並輕微鏡頭震動
    並且 Boss 撞擊的震動更強

  # ---------- Boundary Conditions / 節流 ----------
  場景: 大量擊殺不卡頓
    當 同時大量擊殺與命中
    那麼 擊殺粒子達全域上限時新擊殺只出環波、不出碎屑
    並且 傷害數字達同時存活上限時不再生成新字
    並且 所有特效固定壽命自動回收、FPS 維持正常

  # ---------- Error Handling ----------
  場景: 重新開始清空特效
    當 遊戲結束後重新開始
    那麼 所有殘留特效被清空（destroy 冪等）

  # ---------- 行為保持 ----------
  場景: 玩法與模擬不變
    假設 特效為純視覺
    那麼 碰撞、生怪、難度倍率、傷害結算、entity 造型完全不變
    並且 既有單元測試全數維持通過

  # ---------- Determinism ----------
  場景: 特效不影響確定性
    假設 特效走 renderer clock、鏡頭震動偽隨機僅影響視覺
    那麼 相同 seed 的模擬結果不變

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
