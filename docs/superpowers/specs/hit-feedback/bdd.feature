# language: zh-TW
功能: 打擊反饋特效強化（B3）
  作為玩家
  我希望命中有火花/濺紅、擊殺依病原不同死法
  以便戰鬥更有打擊感與份量

  背景:
    假設 特效全在 EffectsLayer + PixiRenderer 事件偵測
    而且 純呈現層、不碰模擬/確定性、粒子數克制走既有上限
    而且 既有 181 測試不受影響

  # 命中火花/濺紅
  場景: 命中噴火花與體液
    假設 投射物/近戰命中一隻敵人
    當 PixiRenderer 偵測該敵人 hp 下降
    那麼 在撞擊點噴亮火花 + 主題色體液濺射（接 bloom）
    而且 同時仍顯示傷害數字

  # 逐病原死亡
  場景: 不同病原不同死法
    假設 各種病原被擊殺
    當 敵 sprite 消失、呼叫 spawnKill 並帶入 enemyKind
    那麼 細菌體液大濺射、病毒外殼碎片飛散、孢子爆孢、自爆體大爆裂、超級病原加大版

  場景: 既有基礎爆裂保留
    假設 螺旋/噴吐/分裂菌被擊殺
    當 spawnKill 執行
    那麼 呈現既有基礎環波 + 碎屑（或輕度差異）

  # 效能克制
  場景: 高頻命中不爆量
    假設 短時間大量命中/擊殺
    當 粒子達 MAX_PARTICLES 上限
    那麼 略過新增粒子、不掉幀

  # 清理
  場景: 重新開始清掉特效
    假設 場上有命中/死亡粒子
    當 EffectsLayer.destroy
    那麼 所有粒子（含 spawnHit 新增）被清除、無殘留

  # 不變項
  場景: 不影響模擬與既有測試
    假設 加入打擊特效
    當 推進模擬
    那麼 碰撞/傷害/掉落由引擎計、確定性不變
    而且 既有 181 單元測試全綠
