# language: zh-TW
功能: 多地圖（視覺 + 難度）
  作為玩家
  我希望進場前選擇不同地圖
  以便在不同場景與難度下遊玩

  背景:
    假設 遊戲以固定種子開始

  # ---------- Happy Path ----------
  場景: 熔岩地圖敵人更硬
    當 以地圖「lava」、敵人種類 basic 生成一隻敵人
    那麼 該敵人的 hp 等於 basic 基礎 hp × 1.25

  場景: 冰原地圖敵人較脆
    當 以地圖「tundra」生成一隻 basic 敵人
    那麼 該敵人的 hp 等於 basic 基礎 hp × 0.9

  場景: 熔岩地圖生怪更快
    當 以地圖「lava」推進到生怪重置
    那麼 生怪間隔為 spawnInterval(elapsed) × 0.8（比平原短）

  場景: 地圖視覺欄位正確
    當 以地圖「lava」建立 World
    那麼 mapBgColor / mapGridColor / mapGridAlpha 等於 lava 的設定

  場景: Boss hp 疊乘地圖倍率
    當 以地圖「lava」生成第一隻 Boss
    那麼 Boss hp 等於 220 × (1 + 0.5×0) × 1.25

  # ---------- 選圖流程（實機目視） ----------
  場景: 主選單選地圖後開局
    假設 玩家在主選單看到角色排與地圖排
    當 玩家選擇某地圖並按開始遊戲
    那麼 遊戲背景底色與網格顏色為該地圖
    並且 難度倍率套用於本局

  場景: 再玩一次沿用角色與地圖
    假設 玩家以某角色+地圖遊玩並死亡
    當 玩家按「再玩一次」
    那麼 沿用上次的角色與地圖開新局

  # ---------- Boundary Conditions ----------
  場景: 省略地圖參數時預設平原
    當 以 new World(seed) 或 new World(seed, character) 建立（不傳地圖）
    那麼 套用 plains（倍率皆 1、視覺同現況）

  # ---------- Determinism ----------
  場景: 地圖不破壞確定性
    假設 兩局使用相同 seed、角色、地圖、操作
    當 比較模擬結果
    那麼 兩局一致（地圖只改倍率與視覺）

  # ---------- Authorization（單機前端，無權限層） ----------
  場景: 無權限概念
    假設 本功能為純前端單機遊戲
    那麼 不存在需要授權檢查的操作
    並且 此分類標記為 N/A
