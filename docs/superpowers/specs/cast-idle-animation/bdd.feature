# language: zh-TW
功能: 隊伍動態生命感（B2）
  作為玩家
  我希望免疫細胞/病原有待機微動畫（呼吸/搖擺/性格動態）
  以便畫面有生命感與性格，更精緻沉浸

  背景:
    假設 動畫全在 PixiRenderer 每幀對 sprite 套 transform、不重畫造型
    而且 各單位以隨機相位錯開、幅度克制
    而且 不碰模擬/確定性，既有 181 測試不受影響

  # 相位錯開
  場景: 各單位脈動不同步
    假設 多個同種敵人同時在場
    當 推進動畫
    那麼 各自以不同相位脈動/搖擺、不整齊劃一

  # Happy Path（瀏覽器實機看動態）
  場景: 玩家細胞呼吸與搖擺
    假設 玩家在場
    當 推進每幀
    那麼 細胞緩慢呼吸擠壓拉伸、朝向加微搖擺

  場景: 各病原不同動態性格
    假設 場上各種病原
    當 推進每幀
    那麼 細菌游動抖擺、孢子極緩呼吸、自爆體緊張快脈動、超級病原沉重大脈動

  # Boundary / 克制
  場景: 動態不影響碰撞與瞄準
    假設 待機 transform 套在 sprite
    當 單位脈動/位移
    那麼 碰撞仍以 entity.pos 計（視覺位移不影響）
    而且 幅度克制、不致看不清造型或干擾瞄準

  # 效能
  場景: 大量物件仍順
    假設 場上數百敵人
    當 每幀套待機 transform
    那麼 僅每物件幾個 sin/transform、FPS 無明顯影響

  # 不變項
  場景: 不影響模擬與既有測試
    假設 加入待機動畫
    當 推進模擬
    那麼 引擎/store/確定性不變、命中閃白/相機/震動相容
    而且 既有 181 單元測試全綠
